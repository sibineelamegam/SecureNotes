import { Typography, Box } from '@mui/material';

const UserDashboard = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">👤 User Dashboard</Typography>
      <Typography>Welcome, user! Here’s your dashboard.</Typography>
    </Box>
  );
};

export default UserDashboard;
